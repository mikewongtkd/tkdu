
# Skill Graph Manager

PHP + Bootstrap 5 + jQuery + SortableJS + Chart.js

Features:
- JSON file storage
- UUID v4 (crypto.randomUUID in browser)
- Full CRUD
- Skill composition graph
- Many-to-many linked references
- Tree view
- Graph view
- Import/Export JSON
- Cycle detection warning
